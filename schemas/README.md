# Shared Schemas

JSON schemas and typed contracts shared between API, workers, and web.

## Current contents
- `agent_event.schema.json`
- `agent_profile.schema.json`
- `score_snapshot.schema.json`

Codex should add generated TypeScript and Python types once the schema stabilizes.
